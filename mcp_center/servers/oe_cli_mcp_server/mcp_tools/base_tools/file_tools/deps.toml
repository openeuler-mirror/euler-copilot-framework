[system]
# 系统基础工具（grep/sed/awk/sort/uniq/echo 通常系统自带，此处列出需确保安装的依赖）
#required_tools = [
#    "yum install -y grep sed gawk coreutils"
#]
# SSH客户端依赖（部分系统可能未预装）
#ssh_client = [
#    "yum install -y openssh-clients"
#]
[pip_config]
index_url = "https://pypi.tuna.tsinghua.edu.cn/simple"

[pip]
# Python依赖包
paramiko = "==4.0.0"  # SSH远程连接
typing_extensions = "==4.12.2"
psutil = "==7.0.0"
toml =  "== 0.10.2"
mcp =  "== 1.9.4"
scp =  "== 0.15.0"  # 类型提示兼容（如需）