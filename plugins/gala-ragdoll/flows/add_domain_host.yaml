name: 添加业务域主机
description: 添加业务域主机，将业务域未添加的主机添加到业务域中，并将最终添加之后的主机列表查询接口结果以表格形式展示
steps:
  - name: start
    call_type: api
    params:
      endpoint: POST /conftrace/host/addHost
    next: end
  - name: end
    call_type: none
  
