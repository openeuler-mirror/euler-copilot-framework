name: 查看业务域域内配置
description: 查看业务域域内配置，可以查看当前业务域添加的所有基线配置文件和配置文件内容，域内配置列表以表格形式展示
steps:
  - name: start
    call_type: api
    params:
      endpoint: POST /conftrace/management/getManagementConf
    next: end
  - name: end
    call_type: none
