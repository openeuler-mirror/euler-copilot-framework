name: 获取当前未添加的业务域主机
description: 获取当前未添加的业务域主机，过滤出业务域未添加的主机，未添加的主机以表格形式展示
steps:
  - name: start
    call_type: api
    params:
      endpoint: POST /conftrace/host/getNonexistentHost
    next: end
  - name: end
    call_type: none
