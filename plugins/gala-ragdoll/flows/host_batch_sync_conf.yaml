name: 批量同步主机所有配置文件
description: 批量同步主机所有配置文件，可以将业务域所有基线配置文件的内容同步到主机当前所有配置文件中，使得所有配置文件的基线内容和实际内容达到同步状态，将同步结果以表格形式展示
steps:
  - name: start
    call_type: api
    params:
      endpoint: POST /conftrace/confs/syncConf
    next: end
  - name: end
    call_type: none