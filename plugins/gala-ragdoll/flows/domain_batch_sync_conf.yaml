name: 批量同步业务域主机配置
description: 批量同步业务域主机配置，可以将业务域基线配置文件的内容同步到勾选的主机当前所有配置文件中，使得所有配置文件的基线内容和实际内容达到同步状态，并将同步结果以表格形式展示
steps:
  - name: start
    call_type: api
    params:
      endpoint: POST /conftrace/confs/batch/syncConf
    next: end
  - name: end
    call_type: none