name: 查看当前支持的业务域配置
description: 查看当前支持的业务域配置，可以看到当前可以配置的业务域文件，如果业务域配置文件已经被添加，将不会出现在列表中，当前支持的配置文件以表格形式展示
steps:
  - name: start
    call_type: api
    params:
      endpoint: POST /conftrace/confs/querySupportedConfs
    next: end
  - name: end
    call_type: none
