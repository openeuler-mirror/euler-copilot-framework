name: 查看指定业务域域内配置的配置变更日志
description: 查看业务域配置的配置变更日志，可以查看当前单个配置文件的基线内容以及当前基线配置内容的变更历史(变更ID、变更时间、变更人、变更原因、变更详情)，配置变更日志以表格形式展示
steps:
  - name: start
    call_type: api
    params:
      endpoint: POST /conftrace/management/queryManageConfChange
    next: end
  - name: end
    call_type: none
