name: generate_rollback_task
description: 生成CVE回滚任务,传入的任务ID必须存在并且任务已经执行完成,否则任务生成失败,任务信息、任务名称用户可以自定义
steps:
  - name: start
    call_type: api
    params:
      endpoint: POST /vulnerabilities/task/cve-rollback/generate
    next: end
  - name: end
    call_type: none
