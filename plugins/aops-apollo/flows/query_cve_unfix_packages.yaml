name: query_cve_unfix_packages
description: 查看指定CVE ID 用于修复的rpm包信息,只用于查询CVE用于修复的rpm包信息,不适用执行cve修复任务,并用表格形式返回
steps:
  - name: start
    call_type: api
    params:
      endpoint: POST /vulnerabilities/cve/unfixed/packages/get
    next: end
  - name: end
    call_type: none
