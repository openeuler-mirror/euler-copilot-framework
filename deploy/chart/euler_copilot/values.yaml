# 全局设置
globals:
  # 节点架构：默认是x86
  # [必填] 节点设置：["x86", "arm"]
  arch:
  # 镜像拉取策略， 默认为IfNotPresent
  imagePullPolicy:
  # 存储类；默认为local-path
  storageClass:

# 模型设置
models:
  # 用于问答的大模型；需要为OpenAI兼容接口
  answer:
    # 可用的框架类型：["vllm", "sglang", "ollama", "openai"]
    provider: 
    # [必填] 接口URL（请根据 API 提供商文档确定是否需要带上“v1”后缀）
    endpoint:
    # [必填] 接口API Key；默认置空
    key:
    # [必填] 模型名称
    name:
    # [必填] 模型最大上下文数；建议>=8192
    ctxLength: 8192
    # 模型最大输出长度，建议>=2048
    maxTokens: 2048
  # 用于Function Call的模型；建议使用特定推理框架
  functionCall:
    # [必填] 模型地址；请根据 API 提供商文档确定是否需要带上“v1”后缀
    # 选择不填则与问答模型一致
    endpoint:
    # API Key；不填则与问答模型一致
    key:
    # 模型名称；不填则与问答模型一致
    name:
    # 模型最大上下文数；不填则与问答模型一致
    ctxLength:
    # 模型最大输出长度；不填则与问答模型一致
    maxTokens:
  # 用于数据向量化（Embedding）的模型
  embedding:
    # 推理框架类型，默认为openai
    # [必填] Embedding接口类型：["openai", "mindie"]
    provider:
    # [必填] Embedding URL（需要带上“v1”后缀）
    endpoint:
    # [必填] Embedding 模型API Key
    key:
    # [必填] Embedding 模型名称
    name:
  # 用于对rag检索结果重排的模型，支持硅基流动 百炼 vllm asscend等模型供应方提供的api
  reranker:
    # [必填] reranker接口类型：["guijiliudong", "bailian", "v1lm", "assecend"]
    provider:
    # [必填] reranker URL（需要带上“/v1/rerank”后缀）  
    endpoint:
    # [必填] reranker 模型API Key
    key:
    # [必填] reranker 模型名称
    name:

# 登录设置
login:
  # [必填] 登录提供者类型：["authhub", "authelia"]
  provider:
  # AuthHub客户端ID设置，仅在provider为authhub时有效
  client:
    # [必填] 客户端ID
    id:
    # [必填] 客户端密钥
    secret:
  # Authelia OIDC设置，仅在provider为authelia时有效
  authelia:
    # [必填] Authelia服务URL
    url:
    # [必填] OIDC客户端ID（建议使用UUID）
    client_id:
    # [必填] OIDC客户端密钥
    client_secret:

#域名设置
domain:
  # [必填] EulerCopilot的web前端url；默认为http://127.0.0.1:30080
  euler_copilot:
  # [可选] authhub的web前端url
  # 如果不设置，将自动从euler_copilot域名和ports.authhub构建
  # 建议：部署认证服务时，将实际的认证服务地址设置到这里
  authhub:
  # [可选] authelia的web前端url  
  # 如果不设置，将自动从euler_copilot域名和ports.authelia构建
  # 建议：部署认证服务时，将实际的认证服务地址设置到这里
  # 例如：如果认证服务部署在 https://10.211.55.10:30091，请设置此值
  authelia:

# 服务端口设置（仅在domain未设置时用于自动构建域名）
# 注意：这些端口仅作为fallback，实际应该在domain中设置完整的认证服务地址
ports:
  # AuthHub web服务端口；默认为30081
  # 仅在domain.authhub未设置时使用
  authhub: 30081
  # Authelia服务端口；默认为30091  
  # 仅在domain.authelia未设置时使用
  authelia: 30091

# 存储设置
storage:
  # 语义接口
  frameworkSemantics:
    # 语义接口存储路径，默认为/var/lib/eulercopilot/semantics
    path: /var/lib/eulercopilot/semantics
    # 语义接口存储大小，默认为5GB
    size: 5Gi
  # 共享存储
  webAsset:
    # 前后端共享存储路径，默认为/var/lib/eulercopilot/static
    path: /var/lib/eulercopilot/static
    # 前后端共享存储大小，默认为10GB
    size: 10Gi

euler_copilot:
  # 配置文件工具
  secretInject:
    # 镜像设置；默认为hub.oepkgs.net/neocopilot/secret_inject:dev-x86
    # 镜像标签：["dev-x86", "dev-arm"]
    image:

  framework:
    # [必填] 是否部署Framework后端框架服务
    enabled: true
    # 镜像设置；默认为hub.oepkgs.net/neocopilot/euler-copilot-framework:0.10.1-x86
    # 镜像标签：["0.10.1-x86", "0.10.1-arm"]
    image:
    # 容器根目录只读
    readOnly:
    # 性能限制设置
    resourceLimits: {}
    # Service设置
    service:
      # Service类型，例如NodePorts
      type:
      # 当类型为nodePort时，填写主机的端口号
      nodePort:

  web:
    # [必填] 是否部署Web前端用户界面
    enabled: true
    # 镜像设置；默认为hub.oepkgs.net/neocopilot/euler-copilot-web:0.10.1-x86
    # 镜像标签：["0.10.1-x86", "0.10.1-arm"]
    image:
    # 容器根目录只读
    readOnly:
    # 性能限制设置
    resourceLimits: {}
    # Service设置
    service:
      # Service类型，例如NodePort
      type: NodePort
      # 当类型为NodePort时，填写主机的端口号
      nodePort: 30080

  rag_web:
    # [必填] 是否部署RAG Web前端用户界面
    enabled: true
    # 镜像设置；默认为hub.oepkgs.net/neocopilot/data_chain_web:0.10.1-x86
    # 镜像标签：["0.10.1-x86", "0.10.1-arm"]
    image:
    # 容器根目录只读
    readOnly:
    # 性能限制设置
    resourceLimits: {}
    # Service设置
    service:
      # Service类型，例如NodePort
      type:
      # 当类型为NodePort时，填写主机的端口号
      nodePort:

  rag:
    # [必填] 是否部署RAG后端服务
    enabled: true
    # 镜像设置；默认为hub.oepkgs.net/neocopilot/data_chain_back_end:0.10.1-x86
    # 镜像标签：["0.10.1-x86", "0.10.1-arm"]
    image:
    # 容器根目录只读
    readOnly:
    # 性能限制设置
    resourceLimits: {}
    # Service设置
    service:
      # Service类型，例如NodePort
      type:
      # 当类型为NodePort时，填写主机的端口号
      nodePort:

  sandbox:
    # [必填] 是否部署代码沙箱服务
    enabled: true
    # 镜像设置；默认为hub.oepkgs.net/neocopilot/openeuler-intelligence-sandbox:latest-x86
    # 镜像标签：["latest-x86", "latest-arm"]
    image:
    # 镜像版本
    version:
    # 副本数量
    replicas: 1
    # 容器根目录只读
    readOnly:
    # 日志级别
    logLevel: INFO
    # 静默模式
    quietMode: false
    # 安全配置
    security:
      # 启用安全限制
      enabled: true
      # 最大执行时间（秒）
      maxExecutionTime: 30
      # 最大内存使用（MB）
      maxMemoryMB: 512
    # 队列配置
    queue:
      # 最大队列大小
      maxSize: 100
      # 工作线程数
      workerThreads: 4
    # 容器配置
    container:
      # 容器超时时间（秒）
      timeout: 60
      # 内存限制
      memoryLimit: "512m"
      # CPU限制
      cpuLimit: "0.5"
    # 支持的编程语言
    supportedLanguages:
      - python
      - javascript
      - bash
    # 性能限制设置
    resourceLimits: {}
    # Service设置
    service:
      # Service类型，例如NodePort
      type:
      # 当类型为NodePort时，填写主机的端口号
      nodePort:
tls:
  enabled:
  secretName: eulercopilot-tls-secret
